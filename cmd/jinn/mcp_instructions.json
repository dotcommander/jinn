{
  "discover": "Before naming, selecting, recommending, or using a development capability or tool, call jinn_route with the task in need, even when a tool name seems obvious; never infer Jinn tool names from memory. Omit max_tools for adaptive routing. Set include_call=true when arguments are needed. For unrelated questions, do not call Jinn. This route-only profile never executes tools.",
  "read-only": "Before naming, selecting, recommending, or using a development capability or tool, call jinn_route with the task in need, even when a tool name seems obvious; never infer Jinn tool names from memory. Omit max_tools for adaptive routing. Set include_call=true when arguments are needed, then use jinn_call with the recommendation and route_id. For unrelated questions, do not call Jinn. This profile cannot mutate files, state, or execute shell commands.",
  "network": "Before naming, selecting, recommending, or using a development capability or tool, call jinn_route with the task in need, even when a tool name seems obvious; never infer Jinn tool names from memory. Omit max_tools for adaptive routing. Set include_call=true when arguments are needed, then use jinn_call with the recommendation and route_id. For unrelated questions, do not call Jinn. Web calls leave this machine and may consume provider quota; mutation and shell execution are unavailable."
}
